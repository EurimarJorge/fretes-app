import { createTheme } from "@mui/material";


export const appTheme = createTheme({
    palette: {
        mode: "dark",
        primary: { main: "#f5f5f1" },
        secondary: { main: "#2f04caff" },        
        text: { primary: "#f5f5f1" },
    },
});